package yte.summerp.oopjava.inheritance;

/**
 * A class is used when we need to define how the task would be done. An interface is used when we need to know what task has to be done.
 */
public class Bird implements FlyingItem {
	public int maxSpeed() {
		return 0;
	}
}
